package wooteco.subway.admin.service;

// @Service
public class StationService {
	//
	// private StationRepository stationRepository;
	//
	// public StationService(StationRepository stationRepository) {
	// 	this.stationRepository = stationRepository;
	// }
	//
	// public StationResponse findById(Long id) {
	// 	Station station = stationRepository.findById(id).orElseThrow(()
	// 		-> new NoSuchElementException(id + "에 해당하는 station 이 없음"));
	// 	return StationResponse.of(station);
	// }
	//
	// public StationResponse create(StationCreateRequest stationCreateRequest) {
	// 	Station station = stationCreateRequest.toStation();
	// 	Station savedStation = stationRepository.save(station);
	// 	return StationResponse.of(savedStation);
	// }
}
